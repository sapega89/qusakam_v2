# Необходимые аддоны для перенесенных систем

## Обязательные аддоны

### 1. DialogueQuest
**Статус:** ⚠️ **ОБЯЗАТЕЛЬНЫЙ**

**Описание:** Аддон для системы диалогов. Используется в:
- `DialogueManager.gd` - для запуска и управления диалогами
- `SaveSystem.gd` - для сохранения/загрузки флагов диалогов
- `CutsceneDialogueStep.gd` - для диалогов в катсценах

**Где используется:**
- `SampleProject/Scripts/Systems/DialogueManager.gd`
- `SampleProject/Scripts/Systems/SaveSystem.gd`
- `SampleProject/Scripts/Systems/Cutscenes/CutsceneDialogueStep.gd`

**Установка:**
1. Скачайте аддон DialogueQuest из Asset Library или GitHub
2. Поместите в `addons/dialogue_quest/`
3. Включите плагин в Project Settings → Plugins
4. Добавьте в autoload в `project.godot`:
   ```
   DialogueQuest="*res://addons/dialogue_quest/scripts/dialogue_quest_interface.gd"
   ```

**Настройка:**
- Создайте папку `dialogue_quest/dialogues/` для .dqd файлов
- Настройте секцию `[dialogue_quest]` в `project.godot`:
  ```
  [dialogue_quest]
  data_directory="res://dialogue_quest/"
  ```

**Без этого аддона:**
- Диалоги не будут работать
- DialogueManager будет выдавать ошибки
- SaveSystem не сможет сохранять флаги диалогов

---

## Опциональные аддоны

### 2. item_database
**Статус:** ✅ **ОПЦИОНАЛЬНЫЙ** (не требуется)

**Описание:** Аддон для работы с предметами через DTO (Data Transfer Objects).

**Где используется:**
- `ItemDatabase.gd` - опционально для DTO ItemData

**Текущая реализация:**
- `ItemDatabase.gd` уже адаптирован для работы **БЕЗ** этого аддона
- Использует только сырые Dictionary из JSON
- Метод `get_item_data()` возвращает `null`, если аддон не установлен
- Все основные методы работают без аддона

**Если хотите использовать:**
1. Установите аддон `item_database`
2. Раскомментируйте/добавьте в `ItemDatabase.gd`:
   ```gdscript
   const ItemDataScript := preload("res://addons/item_database/ItemData.gd")
   ```
3. Раскомментируйте код создания DTO в методе `load_items()`

**Без этого аддона:**
- ✅ ItemDatabase работает нормально
- ✅ Все методы получения предметов работают
- ❌ Метод `get_item_data()` будет возвращать `null` (но это не критично)

---

### 3. basic_settings_menu
**Статус:** ✅ **ОПЦИОНАЛЬНЫЙ** (можно заменить)

**Описание:** Аддон для UI меню настроек.

**Где используется:**
- `SettingsManager.gd` - для открытия меню настроек

**Текущая реализация:**
- `SettingsManager.gd` ссылается на `res://addons/basic_settings_menu/settings.tscn`
- Можно заменить на собственную реализацию UI настроек

**Альтернативы:**
1. Создать свой UI для настроек
2. Использовать стандартные Godot UI элементы
3. Установить аддон `basic_settings_menu`

**Без этого аддона:**
- ❌ SettingsManager не сможет открыть UI настроек
- ✅ Можно создать свой UI и изменить путь в `SettingsManager.gd`

---

## Уже установленные аддоны

### MetSys (MetroidvaniaSystem)
**Статус:** ✅ **УЖЕ УСТАНОВЛЕН**

**Описание:** Система для метроидвании, уже используется в проекте.

**Где используется:**
- `Game.gd` - для управления комнатами и сохранениями
- Уже настроен в `project.godot`

---

## Итоговый список для установки

### Минимальная конфигурация (только обязательные):
1. ✅ **DialogueQuest** - для диалогов

### Полная конфигурация (с опциональными):
1. ✅ **DialogueQuest** - для диалогов
2. ⚠️ **item_database** - для DTO ItemData (опционально)
3. ⚠️ **basic_settings_menu** - для UI настроек (опционально, можно заменить)

---

## Инструкция по установке DialogueQuest

1. **Скачайте аддон:**
   - Из Asset Library: https://godotengine.org/asset-library/asset/...
   - Или из GitHub репозитория DialogueQuest

2. **Установите:**
   - Поместите папку `dialogue_quest` в `addons/`
   - Или используйте Asset Library → Install

3. **Включите плагин:**
   - Project Settings → Plugins
   - Найдите "DialogueQuest"
   - Включите плагин

4. **Настройте autoload:**
   - Project Settings → Autoload
   - Добавьте:
	 ```
	 DialogueQuest="*res://addons/dialogue_quest/scripts/dialogue_quest_interface.gd"
	 ```

5. **Создайте структуру папок:**
   - Создайте `dialogue_quest/dialogues/` в корне проекта
   - Поместите туда .dqd файлы диалогов

6. **Настройте project.godot:**
   - Добавьте секцию:
	 ```
	 [dialogue_quest]
	 data_directory="res://dialogue_quest/"
	 ```

---

## Проверка установки

После установки DialogueQuest проверьте:

1. **В консоли при запуске должно быть:**
   ```
   💬 DialogueManager: DialogueQuest доступен
   ```

2. **Если видите ошибки:**
   ```
   ⚠️ DialogueManager: DialogueQuest не установлен
   ```
   - Проверьте, что плагин включен
   - Проверьте путь в autoload
   - Проверьте, что файл `dialogue_quest_interface.gd` существует

---

## Примечания

- **ItemDatabase** работает без аддона `item_database` - использует только JSON
- **SettingsManager** можно адаптировать под свой UI настроек
- **DialogueQuest** обязателен для работы диалогов - без него диалоги не будут работать
